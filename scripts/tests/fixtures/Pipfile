[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
requests = ">=2.30.0"
click = ">=8.1.0"

[dev-packages]
pytest = ">=7.4.0"
pytest-cov = ">=4.1.0"
black = ">=23.12.0"
flake8 = ">=6.1.0"
mypy = ">=1.7.0"

[requires]
python_version = "3.9"
